apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-{{ .Values.services.order.appName }}-deployment
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.services.order.appName }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.order.appName }}
        app.kubernetes.io/name: {{ .Values.services.order.appName }}
        app.kubernetes.io/part-of: {{ .Values.services.order.appName }}
        app.kubernetes.io/version: {{ .Values.services.tag  }}
        keptn.sh/post-deployment-tasks: stdout-notification
    spec:
      containers:
        - name: {{ .Values.services.order.appName }}
          image: "{{ .Values.services.registry }}/{{ .Values.services.order.image }}:{{ .Values.services.tag }}"
          imagePullPolicy: Always
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://{{ .Release.Name }}-mysql.{{ .Release.Namespace }}.svc.cluster.local:3306/order_service
            - name: LOKI_URL
              value: http://{{ .Release.Name }}-loki-gateway.{{ .Release.Namespace }}.svc.cluster.local:80/loki/api/v1/push
            - name: INVENTORY_URL
              value: {{ .Release.Name }}-inventory-service.{{ .Release.Namespace }}.svc.cluster.local
            - name: SPRING_KAFKA_BOOTSTRAP-SERVERS
              value: {{ .Release.Name }}-kafka.{{ .Release.Namespace }}.svc.cluster.local
            - name: SPRING_KAFKA_PRODUCER_PROPERTIES_SCHEMA_REGISTRY_URL
              value: {{ .Release.Name }}-schema-registry.{{ .Release.Namespace }}.svc.cluster.local
            - name: MANAGEMENT_ZIPKIN_TRACING_ENDPOINT
              value: http://{{ .Release.Name }}-tempo-distributor-.{{ .Release.Namespace }}.svc.cluster.local:9411
          ports:
            - containerPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.order.appName }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Values.services.order.appName }}
  ports:
    - port: 8081
      targetPort: 8081